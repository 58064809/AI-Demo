# 每日 KPI 项目工作说明

这个项目用于每天生成运动生活类内容 KPI：文案、标签、图片提示词，以及按日期保存的配图结果。以后进入本项目时，默认按下面规则执行，不需要用户每天重复说明。

## 默认任务

每天生成一组运动内容：

- 从 `prompts/sports_daily_prompts.json` 的 `sports` 中随机选择 3 个运动项目，不能重复。
- 当前 `sports` 只包含：羽毛球、飞盘、匹克球、网球。
- 每个已选运动各自从 `topic_pool` 中随机匹配 1 个话题方向。
- 每天默认产出 3 组 sport/topic 内容，除非用户另有指定。
- 每条内容包含标题、图片提示词、分段文案、标签。
- 每组内容默认生成 4 张图，每天共 12 张图，保存到 `generated_images/YYYY-MM-DD/`。
- 当天文案汇总保存为 `generated_images/YYYY-MM-DD/文案.md`。

## 内容风格

- 文案要像真实用户分享，生活化、松弛、有细节。
- 避免官方通稿、课程广告、营销腔和机械三段式。
- 正文至少分 2 到 3 段，段落之间空行分隔。
- 生成后按 `humanizer-zh` 的标准检查一遍，删掉套话、过度总结、不自然连接词和明显 AI 味。
- 图片提示词根据当前运动和话题自动判断更合适的视觉形式，不再固定为真实手机随手拍、生活感、纪实摄影。
- 图片获取优先级：先从网上查找清晰、相关、可复用且来源明确的图片；网络图不合适、缺少授权信息、清晰度不够或需要特定创意画面时，再使用 `gpt-image-2-style-library` 技能整理 GPT-Image2 提示词并生成图片。
- 可用视觉方向包括：真实生活感照片、手机随手拍、球场日常、练习/约球/下班运动场景、漫画/插画风、轻松有趣的梗图感配图、教程图/教学感图片、真实赛事图/选手图/现场氛围图，以及其他更符合当前话题的视觉形式。
- 默认判断逻辑：新手入门、小白体验、工作日运动穿搭、练后好物分享偏真实生活感照片；零基础教学、技能干货偏教程图/教学感图片；赛事信息集结偏真实赛事图/选手图/现场氛围图；圈子乱象吐槽、圈子八卦分享与讨论偏漫画/插画/梗图感配图，也可以根据内容改用真实生活场景图。
- 图片不要太像硬广，尽量自然、适合发动态。
- 网络图优先来源：Pexels、Unsplash、Wikimedia Commons、官方赛事/场馆/协会图片，以及其他授权清楚、来源可信的图片页面。
- 图片清晰度要求：避免缩略图、低清图、严重压缩图和模糊图；优先长边至少 1600px，最低不低于 1200px；人物、球拍、球、场线、记分牌等关键元素要清楚。
- 使用网络图时记录来源 URL、作者/平台、许可证或使用说明；Wikimedia Commons 必须记录具体文件页和许可证。不要使用授权不明、明显水印、营销海报、截图搬运或可能侵权的图片。
- 题材可以覆盖新手入门、小白体验、零基础教学、技能干货、赛事信息、穿搭、练后好物、圈子吐槽和圈子八卦讨论，不需要刻意回避基础内容。

## 输出格式

当天 `文案.md` 建议使用下面格式：

```markdown
# 运动｜具体标题

## 图片

1. 图片提示词 1
2. 图片提示词 2
3. 图片提示词 3
4. 图片提示词 4

## 文案

第一段文案。

第二段文案。

## 标签

#话题标签 #运动标签 #生活化标签
```

多条内容之间用 `---` 分隔。

## 文件约定

- `prompts/sports_daily_prompts.json` 是内容规则和素材池。
- `generated_images/YYYY-MM-DD/` 是每天的输出目录。
- 如果使用网络图，当天目录同时保存 `图片来源.md`，记录每张图的来源和授权信息。
- 图片命名建议使用英文运动名加序号，例如 `badminton_01.png` 到 `badminton_04.png`。
- 文本文件统一使用 UTF-8 编码，避免中文乱码。

## 工作方式

- 开始前先查看当天目录是否已存在，避免覆盖已有成果。
- 如果当天已有部分内容，优先补齐缺失项，而不是重做全部。
- 除非用户明确要求，不删除历史日期目录。
- 用户只说“继续做今天的”或“生成今天的 KPI”时，直接按本文件执行。
